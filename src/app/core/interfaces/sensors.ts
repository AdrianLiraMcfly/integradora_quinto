export interface sensorsData {
    data: Sensors[]
  }
  
  export interface Sensors {
    id: number
    name: string
    status: number
  }

  export interface Newsensor {
    name: string
    status: number
  }
